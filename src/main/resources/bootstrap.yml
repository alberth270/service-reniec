spring:
  application:
    name: reniec
  
  cloud:
    config:
      uri: http://localhost:8080  
management:
  endpoints:
    web:
      exposure:
        include: "*"
server:
  port: 9002

entityName: Reniec
success: true